---
# Wmonk project configuration file

# Title of project
<%= title.to_yaml.sub(/^---\n/, '').sub(/\n$/, '') %>

# Root URLs
<%= root_urls.to_yaml.sub(/^---\n/, '').sub(/\n$/, '') %>

# URLs to initially request
<%= seed_urls.to_yaml.sub(/^---\n/, '').sub(/\n$/, '') %>
